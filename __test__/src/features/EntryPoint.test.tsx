// Testing Library
import { render } from '@testing-library/react-native'

// Components
import { EntryPoint } from '@/EntryPoint'

it('EntryPoint should be rendered', () => {
	render(<EntryPoint />)
})
