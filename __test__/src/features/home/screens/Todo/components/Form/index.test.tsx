// Testing Library
import {render} from '@testing-library/react-native';

// Components
import {Form} from '@/features/home/screens/Todo/components/Form';

it('Form should be rendered', () => {
  // render(<Form />);
});
