// Testing Library
import {render} from '@testing-library/react-native';

// Components
import {Item} from '@/features/home/screens/Todo/components/Item';

it('Item should be rendered', () => {
  // render(<Item />);
});
